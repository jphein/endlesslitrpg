[package]
name = "litrpg-config"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Configuration loading for endless-litrpg, shared by the CLI, daemon and engine"

[dependencies]
serde = { workspace = true, features = ["std"] }
toml = { workspace = true }
thiserror = { workspace = true }
dirs = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

